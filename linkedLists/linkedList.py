# singly linked list implementation in python

class Node:
  def __init__(self, val):
    self.val = val
    self.next = None

class LinkedList:
  def __init__(self, head):
    self.head = head

  def insert(self, node):
    # inserts a node at the proper position according to its value
    return

  def delete(self, value):
    # delete a node/nodes with a particular value
    return

  def reverse(self):
    # reverse the list
    return

  def hasCycle(self):
    # checks if a cycle exists in the list
    return

  def length(self):
    # gets the length of the linked list
    return

  def insertNodes(self, nodes):
    # inserts a list of nodes at their proper positions
    return